# Campaign reports

Campaign outputs use deterministic JSON and CSV representations plus SHA-256
manifests. Runs include formulation, method/version, backend/version, stochastic
seed when applicable, parameters, objective, timing, BKS comparison and
historical declared/detected audit.
